__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Datasets are downloaded, not committed.
data/*.csv
data/*.npz

# Run artifacts
checkpoints/
outputs/
runs/
logs/
# Result JSONs ARE tracked -- they are the source behind every table in
# RESULTS.md. Only the checkpoints beside them are too large to keep.
*.pt
*.pth
*.log

experiments/results_ssl_prerun/

# Throwaway 2-3 epoch smoke runs, not results.
experiments/results_ssl/smoke_*.json
